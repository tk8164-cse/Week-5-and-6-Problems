import java.util.Scanner;
public class DigitFrequency {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        System.out.print("Enter a number: ");
        long number = scanner.nextLong(); // Using long for larger numbers
        long tempNum = Math.abs(number);
        long originalNum = tempNum;
        int count = 0;
        if (tempNum == 0) count = 1;
        while (tempNum > 0) {
            tempNum /= 10;
            count++;
        }
        int[] digitsArray = new int[count];
        tempNum = originalNum;
        for (int i = 0; i < count; i++) {
            digitsArray[i] = (int)(tempNum % 10);
            tempNum /= 10;
        }
        int[] frequency = new int[10];
        for (int i = 0; i < digitsArray.length; i++) {
            int digit = digitsArray[i];
            frequency[digit]++; // Increment the value at the index matching the digit
        }
        System.out.println("\nDigit Frequency Table:");
        System.out.println("Digit | Frequency");
        System.out.println("---------------");
        for (int i = 0; i < frequency.length; i++) {
            if (frequency[i] > 0) { // Only display digits that appeared
                System.out.println("  " + i + "   |    " + frequency[i]);
            }
        }
        scanner.close();
    }
}
